interface:
  display_name: "Fifoland"
  short_description: "Repo-specific patterns and workflows for Fifoland"
  default_prompt: "Use the Fifoland repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true